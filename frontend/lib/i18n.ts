export const locales = ["en", "hi"] as const;
export type Locale = (typeof locales)[number];

export const messages = {
  en: {
    privacyTitle: "Privacy and data control center",
    exportData: "Request secure data export",
    deleteAccount: "Request account deletion",
    onboardingTitle: "Start safely with HealthGuard AI",
  },
  hi: {
    privacyTitle: "???????? ?? ???? ???????? ??????",
    exportData: "???????? ???? ??????? ?? ?????? ????",
    deleteAccount: "???? ????? ?? ?????? ????",
    onboardingTitle: "HealthGuard AI ?? ??? ???????? ?????? ????",
  },
} satisfies Record<Locale, Record<string, string>>;

export function formatDateTime(value: string | Date, locale: Locale = "en") {
  return new Intl.DateTimeFormat(locale === "hi" ? "hi-IN" : "en-US", { dateStyle: "medium", timeStyle: "short" }).format(new Date(value));
}
